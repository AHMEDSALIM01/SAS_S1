#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    printf("entrer la valeur en decimal de a=");
    scanf("%d",&a);
    printf("a en octal=%o,a en hexadeximal a=%x",a,a);

    return 0;
}
